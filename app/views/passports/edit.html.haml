%h1= t("controller.#{params['controller']}.#{params["action"]}")

= render 'form'

= link_to t('button.show'), @passport, class: "btn btn-info"
\|
= link_to t('button.back'), passports_path, class: "btn btn-danger"
