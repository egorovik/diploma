%h1= t("controller.#{params['controller']}.#{params["action"]}")

= render 'form'

= link_to t('button.show'), @country, class: "btn btn-info"
\|
= link_to t('button.back'), countries_path, class: "btn btn-danger"
